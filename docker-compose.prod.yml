services:
  exambot:
    image: exambot-server:latest
    container_name: exambot-server
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - API_KEY=${API_KEY}
      - PORT=3000
      - RUST_LOG=info
    restart: unless-stopped
