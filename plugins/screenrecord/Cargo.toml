[package]
name = "tauri-plugin-screenrecord"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-screenrecord"

[dependencies]
tauri = { version = "2", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }
